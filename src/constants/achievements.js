// Real achievements are managed through the admin panel and stored in
// Supabase. Add entries here only if you want something bundled into the
// site itself regardless of the database.
const Achievements = [];

export default Achievements;
